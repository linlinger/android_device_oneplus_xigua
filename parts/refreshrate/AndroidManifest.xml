<?xml version="1.0" encoding="utf-8"?>
<!--
     Copyright (C) 2026 Oplus Project
     SPDX-License-Identifier: Apache-2.0
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.oplus.refreshrate">

    <!-- 前台服务 (Android 12+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- 前台服务类型 specialUse (Android 14+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- 隐藏 secondary display 需要绑 vendor AIDL (platform 签名已隐含) -->

    <application
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Material.Light">

        <!-- 设置界面 (通过 IA_SETTINGS 集成到系统设置) -->
        <activity
            android:name=".settings.RefreshRateSettingsActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="com.android.settings.action.IA_SETTINGS" />
            </intent-filter>
        </activity>

        <!-- 调度服务 (前台服务) -->
        <service
            android:name=".RefreshRateService"
            android:exported="true"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="refresh_rate_scheduler" />
        </service>

    </application>
</manifest>
